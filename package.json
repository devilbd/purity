{
  "name": "purity",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:dev": "tsc && vite build --mode development",
    "build:prod": "tsc && vite build --mode production",
    "preview": "vite preview",
    "deploy": "bash scripts/build-and-deploy.sh",
    "deploy:hosting": "bash scripts/build-and-deploy.sh --only hosting",
    "build:deploy": "bash scripts/build-and-deploy.sh"
  },
  "devDependencies": {
    "@types/prismjs": "^1.26.6",
    "sass": "^1.103.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "prismjs": "^1.30.0",
    "sucrase": "^3.35.1"
  }
}
